<%= form_with(model: invite_importer, local: true) do |form| %>
  <% if invite_importer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(invite_importer.errors.count, "error") %> prohibited this invite_importer from being saved:</h2>

      <ul>
      <% invite_importer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
