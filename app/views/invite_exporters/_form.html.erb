<%= form_with(model: invite_exporter, local: true) do |form| %>
  <% if invite_exporter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(invite_exporter.errors.count, "error") %> prohibited this invite_exporter from being saved:</h2>

      <ul>
      <% invite_exporter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
