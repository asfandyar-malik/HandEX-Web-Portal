<%= form_with(model: tradeinfo, local: true) do |form| %>
  <% if tradeinfo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tradeinfo.errors.count, "error") %> prohibited this tradeinfo from being saved:</h2>

      <ul>
      <% tradeinfo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
